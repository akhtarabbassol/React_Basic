import React from 'react';
import {useEffect,useState} from 'react';import {Link} from 'react-router-dom';import api from '../services/api';
export default function Orders(){const [orders,setOrders]=useState([]);useEffect(()=>{api.get('/orders/my').then(r=>setOrders(r.data))},[]);return <section className="container page"><span className="eyebrow">ACCOUNT</span><h1>My orders</h1><div className="orders-list">{orders.map(o=><Link className="order-row" to={`/orders/${o._id}`} key={o._id}><div><b>Order #{o._id.slice(-8).toUpperCase()}</b><span>{new Date(o.createdAt).toLocaleDateString()}</span></div><div><span className={`status ${o.status.toLowerCase()}`}>{o.status}</span><b>Rs. {o.total.toLocaleString()}</b></div></Link>)}{!orders.length&&<div className="empty-card">No orders yet.</div>}</div></section>}
